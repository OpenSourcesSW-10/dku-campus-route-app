# GitHub 기준 대비 로컬 수정 사항 정리

## 기준

- GitHub 기준 브랜치: `origin/dev`
- GitHub 기준 커밋: `e439cf7`
- 현재 로컬 브랜치: `feature/engineering-route-display-fix`
- 현재 상태: 로컬에서 수정된 내용이 있으며, 아직 커밋되지 않은 변경사항 포함
- `main` 브랜치: 수정하지 않음

## 전체 요약

현재 로컬 코드는 GitHub의 `dev` 기준 코드에 비해 다음 기능이 추가/수정된 상태입니다.

- 공학관 3개 건물의 경로 계산 규칙 보정
- 1공학관과 3공학관이 외부에서 직접 연결되지 않도록 수정
- 1공학관과 3공학관 이동 시 2공학관을 반드시 거치는 후보 경로 추가
- ICT관/도서관에서 공학관으로 이동할 때 편한 길/비 오는 날 경로가 2공학관 실내 연결 및 구름다리를 우선 고려하도록 DCF 보정
- 경로 단계 UI에 `전체 경로` 단계 추가
- 실내 경로 단계에서 내부 지도 확대 표시 적용
- 공학관 강의실 검색 및 강의실 정보 표시 지원

## 백엔드 코드 변경

### `backend/app/services/route_planner.py`

경로 계산 로직이 가장 많이 수정된 파일입니다.

변경 내용은 다음과 같습니다.

- 공학관 전용 경로 후보 생성 로직 추가
- 1공학관과 3공학관 사이의 직접 외부 이동 후보 제거
- 1공학관과 3공학관 이동 시 2공학관을 경유하는 경로 후보 추가
- 2공학관 내부 이동 후 구름다리를 이용하는 후보 경로 추가
- ICT관/도서관에서 공학관으로 이동할 때 편한 길/비 오는 날 경로가 실내 연결과 구름다리를 더 적극적으로 선택하도록 비용 보정 추가
- 경로 타입별 DCF 결과가 서로 다르게 나오도록 후보 경로와 비용 계산 기준 보강

적용된 핵심 의도는 다음과 같습니다.

- `기본 경로`: 일반적인 외부 이동 경로 우선
- `편한 길`: 계단 부담을 줄이고 실내/구름다리 경로를 더 선호
- `비 오는 날`: 비를 덜 맞는 실내/구름다리 경로를 가장 강하게 선호

### `backend/app/routers/rooms.py`

강의실 검색과 조회 기능이 보강되었습니다.

변경 내용은 다음과 같습니다.

- 공학관 강의실도 검색 결과에 포함되도록 처리
- 건물명, 건물 별칭, 호실 조합 검색 지원
- `제1공학관301`, `제2공학관301`, `제3공학관319` 같은 입력을 백엔드 DB 기반으로 찾을 수 있도록 보강
- 프론트에서 강의실 상세 정보를 표시할 수 있도록 응답 데이터 보완

## DB CSV 변경

### `backend/data/final/외부 구조 설계/csv/outdoor_node.csv`

공학관 외부 경로 계산에 필요한 외부 노드가 추가/정리되었습니다.

포함된 주요 노드는 다음과 같습니다.

- 1공학관 출입구 주변 노드
- 2공학관 출입구 주변 노드
- 3공학관 출입구 주변 노드
- 공학관 사이 평지 연결 노드
- 공학관과 도서관 사이 계단 구간 노드
- 중앙 계단 및 외부 이동 기준점 노드

### `backend/data/final/외부 구조 설계/csv/outdoor_edge.csv`

외부 간선 데이터가 수정되었습니다.

변경 내용은 다음과 같습니다.

- 공학관 사이 외부 경로용 간선 정리
- 잘못된 1공학관-3공학관 직접 연결 경로 제거
- 1공학관과 3공학관은 2공학관 또는 계단 경로를 통해서만 이동하도록 보정
- 경로가 지도 위에서 직선으로만 보이지 않도록 `polyline_points` 기반 경로 좌표 유지
- 계단, 경사, 지붕 여부 등 DCF 계산에 필요한 속성 유지

### `backend/data/final/외부 구조 설계/csv/entrance_links.csv`

건물 출입구와 외부 노드를 연결하는 데이터가 보강되었습니다.

변경 내용은 다음과 같습니다.

- 1공학관 1층/2층 출입구 연결
- 2공학관 1층/2층 출입구 연결
- 3공학관 1층 출입구 연결
- 외부에서 들어온 뒤 내부 층 이동을 할 수 있도록 출입구-실내 노드 연결 보정

## 프론트엔드 변경

### `frontend/src/pages/RouteFind.tsx`

길찾기 화면의 경로 단계 표시가 수정되었습니다.

변경 내용은 다음과 같습니다.

- 경로 단계 맨 앞에 `전체 경로` 선택지 추가
- 전체 경로 선택 시 외부 지도에서 전체 이동 경로 표시
- 실내 이동, 외부 이동, 층간 이동 단계를 분리해서 표시
- 엘리베이터/계단/구름다리 같은 층간 이동 단계 표시 보강
- 공학관 강의실 검색 후보 추가
- 공학관 강의실 간 이동 시 단계별 안내가 나오도록 수정

### `frontend/src/features/indoor/IndoorMap.tsx`

실내 지도 표시 방식이 수정되었습니다.

변경 내용은 다음과 같습니다.

- 실내 지도를 처음 열 때 피난 안내도 전체가 작게 보이지 않도록 초기 확대 적용
- 좌표 자체를 자르지 않고, 표시 시점의 줌/중심만 조정
- 실내 경로 표시 시 경로 주변으로 자동 확대
- 출발/도착/경로 포인트가 있는 영역을 기준으로 화면 중심 계산
- 실내 경로 점 표시 색상을 빨간색 계열로 변경

### `frontend/src/lib/api.ts`

백엔드 API 호출 함수가 보강되었습니다.

변경 내용은 다음과 같습니다.

- 층 목록 조회 API 호출 지원
- 실내 지도 조회 API 호출 지원
- 강의실 검색 API 호출 지원

### `frontend/src/lib/data.ts`

프론트의 로컬 목업 데이터와 백엔드 API 데이터를 같이 사용할 수 있도록 수정되었습니다.

변경 내용은 다음과 같습니다.

- 백엔드에서 층별 실내 지도 정보를 가져와 기존 데이터와 병합
- 백엔드 강의실 검색 결과를 기존 검색 결과와 병합
- 공학관 강의실도 검색 결과에 포함될 수 있도록 처리

### `frontend/src/pages/IndoorMapPage.tsx`

실내 지도 페이지가 백엔드 데이터를 더 적극적으로 사용하도록 수정되었습니다.

변경 내용은 다음과 같습니다.

- 공학관 건물 선택 지원
- 백엔드에서 층 목록을 받아와 표시
- 강의실 위치 정보가 있으면 상세 정보 카드에 표시
- 실내 지도와 강의실 좌표 정보를 함께 표시

### `frontend/src/pages/PlaceSearch.tsx`

검색 화면이 백엔드 강의실 데이터를 포함하도록 수정되었습니다.

변경 내용은 다음과 같습니다.

- 기존 로컬 검색 결과와 백엔드 검색 결과를 함께 표시
- 공학관 강의실 검색 가능

## 로컬 백업 폴더

현재 로컬에는 다음 백업 폴더가 있습니다.

- `_backup_before_5building_route/`

이 폴더는 5개 건물 경로 수정 전 상태를 보관하기 위한 백업용 폴더입니다.

주의할 점은 다음과 같습니다.

- 제출용 커밋에는 포함하지 않는 것이 안전함
- 실제 기능 코드에는 필요하지 않음
- 되돌리기가 필요할 때 참고용으로만 사용

## 검증한 내용

로컬에서 확인한 내용은 다음과 같습니다.

- 백엔드 Python 문법 검사 통과
- 프론트엔드 `npm run build` 통과
- Swagger 접속 가능
- `/api/buildings` 응답 확인
- 공학관 강의실 검색 데이터 확인
- 1공학관-3공학관 경로에서 2공학관을 경유하는 후보 경로 확인
- ICT관에서 3공학관 이동 시 편한 길/비 오는 날 경로가 2공학관 및 구름다리 후보를 고려하도록 확인

## 테스트 시 주의사항

DB CSV를 수정한 경우 기존 SQLite DB가 남아 있으면 수정된 CSV가 반영되지 않을 수 있습니다.

테스트할 때는 기존 테스트 DB를 삭제한 뒤 다시 실행해야 합니다.

```cmd
cd /d "D:\과제\3-2\오픈소스SW기초\#Project\Project\dku-campus-route-app-final-integration\backend"
del test_route_stage.db
set DATABASE_URL=sqlite:///./test_route_stage.db
set IMPORT_DATA_ON_START=true
set DEPLOYMENT_DATA_ROOT=data/final
python -m uvicorn app.main:app --host 127.0.0.1 --port 8010
```

프론트 테스트는 별도 터미널에서 실행합니다.

```cmd
cd /d "D:\과제\3-2\오픈소스SW기초\#Project\Project\dku-campus-route-app-final-integration\frontend"
set VITE_API_BASE_URL=http://127.0.0.1:8010
set VITE_KAKAO_JS_KEY=358ccf6eb949d86269804dd66fd1f851
npm.cmd run dev -- --host 127.0.0.1 --port 5190
```

접속 주소는 다음과 같습니다.

```text
http://127.0.0.1:5190
```

## 현재 커밋 전 확인 필요

GitHub에 올리기 전 확인해야 할 사항은 다음과 같습니다.

- `_backup_before_5building_route/` 폴더는 커밋 제외
- 테스트용 SQLite DB 파일은 커밋 제외
- 프론트/백엔드가 같은 API 주소를 바라보는지 확인
- 카카오 JavaScript 키 도메인에 로컬 테스트 주소가 등록되어 있는지 확인
- 최종 배포 시 Render 환경변수 `DEPLOYMENT_DATA_ROOT=data/final` 유지

## 결론

현재 로컬 수정본은 GitHub 기준 코드보다 경로 계산, 공학관 연결 구조, 실내 지도 확대 표시, 강의실 검색/정보 표시가 더 반영된 상태입니다.

특히 핵심 차이는 다음입니다.

- 기존 GitHub 코드: 공학관 경로가 단순 연결되거나 일부 경로가 부정확할 수 있음
- 현재 로컬 코드: 공학관 지형, 층수 차이, 2공학관 경유, 구름다리, 실내/외부 단계 표시를 더 반영함

따라서 최종 제출 전에는 현재 로컬 수정본을 테스트한 뒤, 백업 폴더와 테스트 DB를 제외하고 커밋하는 것이 적절합니다.
